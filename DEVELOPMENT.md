# 龍安棒球隊網站：離線（本機）開發手冊

這份手冊教你在自己的電腦上修改、測試網站，**完全不動到正式網站與正式 Google 試算表**。

> 📌 **Claude 做好一個 PR，要你「本機測試 → Merge 上線」時，直接看[第 11 節](#11-claude-做好-pr-之後本機測試--merge-上線)。**

## 先搞懂：三個環境

| 環境 | 網址 | 資料寫到哪裡 | 誰看得到 |
|---|---|---|---|
| ① 本機（這份手冊的重點） | `http://localhost:8080` | 假試算表 `dev/data/sheets.json` | 只有你（和同 Wi-Fi 的手機） |
| ② 雲端測試（可選，第 8 節） | `localhost` 網址後面加 `?api=測試GAS網址` | 「試算表副本」 | 只有你 |
| ③ 正式 | `a24627622-dev.github.io/...` | 正式 Google 試算表 | 所有人 |

- **正式網站只有在你 `git push` 到 GitHub 的 main 分支後才會更新**，在本機怎麼改都不會影響正式網站。
- **Apps Script 也一樣**：只有你把程式貼進正式 Apps Script，並按「部署 → 建立新版本」，後端才會變。

`lineup.html` 會自動判斷現在的環境，並在頁首顯示標籤：

- 紫色「🧪 本機假資料」：安全，資料只會寫進本機的假試算表。
- 紅色「⚠️ 正式資料」：在本機開啟，但連的是正式後端（網址加了 `?api=prod`）。**這時操作會寫進正式試算表**，除非你確定要這樣做，否則不要操作。
- 沒有標籤：代表這是正式網站。

---

## 1. 一次性準備（只要做一次）

### 1-1 安裝 Node.js

1. 到 https://nodejs.org 下載 **LTS** 版本，一路按「Next」安裝即可（預設選項就好）。
2. 安裝完，**把 VS Code 完全關掉再重新打開**，終端機才抓得到新安裝的 Node.js。
3. 在 VS Code 上方選單點「終端機（Terminal）→ 新增終端（New Terminal）」，輸入下面這行後按 Enter：
   ```
   node -v
   ```
   有出現版本號（例如 `v22.x.x`）就代表安裝成功。

> 不需要執行 `npm install`，這套本機環境沒有使用任何額外套件。

### 1-2 用 VS Code 開啟專案

1. VS Code →「檔案 → 開啟資料夾」→ 選 `C:\Documents_HTML development\LongAn_baseball_roster`。
2. 左側檔案總管應該看得到下面這些東西：
   ```
   lineup.html      陣容調度助手
   index.html       首頁
   gas/             後端程式（Code.gs、Auth.gs、Pitchers.gs）
   dev/             本機開發伺服器（server.js、config.json、roster.json、start.bat）
   tests/           自動測試
   DEVELOPMENT.md   這份手冊
   ```

### 1-3 先把工作切到「開發分支」（建議）

這一步的目的，是讓正式網站用的 main 分支保持乾淨。在 VS Code 終端機輸入：

```
git switch -c dev
```

- 你目前還沒存檔進版本紀錄（commit）的修改，會一起帶到 `dev` 分支。
- 之後在 `dev` 上怎麼 commit 都不會影響正式網站。
- 如果出現「already exists」，代表 dev 分支已經有了，改用 `git switch dev`。

---

## 2. 每次開發：啟動本機伺服器

以下兩種方式選一種：

**方式 A：直接點兩下（最簡單）**

在檔案總管打開 `dev` 資料夾，點兩下 `start.bat`。

- 會跳出一個黑色視窗，那就是伺服器，**開發期間不要關掉它**。
- 瀏覽器會自動開啟陣容調度助手。如果頁面顯示「無法連線」，等一秒再按重新整理就好。

**方式 B：用 VS Code 終端機**

```
node dev/server.js
```

兩種方式啟動成功後，都會看到類似這樣的訊息：

```
⚾ 龍安棒球隊 本機開發伺服器已啟動
   陣容調度助手：http://localhost:8080/lineup.html
   首頁：        http://localhost:8080/
   假試算表檢視：http://localhost:8080/dev
   手機（同一個 Wi-Fi）：http://192.168.x.x:8080/lineup.html
   隊務密碼：longan-dev　強制登入：false
```

**要停止伺服器時**：在那個視窗按 `Ctrl + C`，或直接關掉視窗。

> ⚠️ 請一定要用 `http://localhost:8080/...` 開啟頁面。不要直接在檔案總管點兩下 `lineup.html`（網址會變成 `file:///...`），那樣抓不到本機後端。

---

## 3. 操作與檢查

1. 開啟 http://localhost:8080/lineup.html。
2. **先確認頁首有紫色「🧪 本機假資料」標籤**。
3. 按「同步名單」，會載入 `dev/roster.json` 裡的測試球員（16 位，其中 4 位是投手）。
4. 照平常的流程操作：點名 → 排先發 → 上傳 → 調度 → 上傳 → 比賽完成。
5. 另外開一個分頁，進入 http://localhost:8080/dev，就能看到「假試算表」目前的內容：比賽分頁、打者表、投手表、調度紀錄。
   - 公式儲存格會顯示成 `ƒ`，滑鼠移上去可以看公式內容。
   - 每次上傳完，按這頁的「重新整理」就會看到最新內容。
6. 想重新開始時：
   - 在 `/dev` 頁面按「清空假資料」（球員名單不會被清掉）。
   - 在 lineup.html 按「🗑️ 重置」，清掉瀏覽器裡的草稿。

伺服器的黑色視窗也會即時顯示每一次請求，例如：

```
POST /gas  action=saveStarters  → ✅ 同步成功！
POST /gas  action=login  → ❌ AUTH_FAILED 密碼錯誤。
```

---

## 4. 修改程式後怎麼看到結果

| 你改了什麼 | 要做什麼 |
|---|---|
| `lineup.html`、`index.html`、`style.css` | 存檔 → 瀏覽器按 `Ctrl + F5`（強制重新整理） |
| `gas/*.gs`（後端） | 存檔就好，下一次請求會自動用新程式，**不用重開伺服器** |
| `dev/config.json`、`dev/roster.json` | 存檔就好（名單改完要在頁面按「同步名單」） |
| `dev/server.js`、`tests/gas_mock.js` | 按 `Ctrl + C` 停止伺服器，再重新啟動 |

**後端程式出錯時**，伺服器視窗會印出紅色的 `❌ doPost(...) 發生錯誤` 和錯誤行號，網頁上也會跳出錯誤訊息。

---

## 5. 測試「隊務登入」

1. 打開 `dev/config.json`，把 `"AUTH_ENFORCED": false` 改成 `true`，然後存檔。
2. 在 lineup.html 按「🔒 登入」，或直接上傳，會跳出密碼視窗。
3. 本機的測試密碼是 `longan-dev`（可以在 config.json 改）。
4. 建議測這幾種情況：
   - **密碼錯誤**：應該顯示「密碼錯誤」。
   - **連錯 10 次**：應該被鎖住。重開伺服器就會解鎖。
   - **按取消**：資料不會送出，草稿要還在。
   - **登入成功**：之前那筆上傳會自動重送。
   - **按「🔓 隊務」登出**：再上傳一次，應該要重新登入。
5. 測完把 `AUTH_ENFORCED` 改回 `false`，平常開發比較方便。

> 本機的密碼只存在 `dev/config.json`，跟正式 GAS 的 `TEAM_PASSCODE` 完全無關，**不要把正式密碼寫進這個檔案**（這個檔案會被 commit 進 git）。

---

## 6. 用手機測試（同一個 Wi-Fi）

1. 電腦和手機連到同一個 Wi-Fi。
2. 看伺服器啟動訊息裡的「手機（同一個 Wi-Fi）」網址，例如 `http://192.168.1.23:8080/lineup.html`，用手機瀏覽器開啟。
3. 手機頁首也應該要顯示紫色「🧪 本機假資料」。
4. 如果手機連不上：
   - 第一次啟動時，Windows 可能跳出「允許 Node.js 存取網路」，請勾選「私人網路」並按允許。
   - 如果當時按了取消：到「Windows 安全性 → 防火牆與網路保護 → 允許應用程式通過防火牆」，把 Node.js 的「私人」打勾。
   - 公司或公共 Wi-Fi 常常會阻擋裝置之間互相連線，改用家裡的 Wi-Fi 或手機熱點試試。

> 手機和電腦的草稿各自獨立（存在各自的瀏覽器裡）。要測「換一支手機接手」，就用「☁️ 回讀雲端歷史紀錄」。

---

## 7. 跑自動測試

改完程式後，在 VS Code 終端機（伺服器不用關，另外開一個終端機）執行：

```
node tests/test_gas_modules.js
node tests/test_lineup_logic.js
```

- 全部顯示 ✅ 就代表通過。如果出現 ❌，下方會寫出是哪一項、哪裡不對。
- 這兩支測試會跑一般 9 人、全場 DH、DH 中途取消、回讀、比賽完成、登入等 47 個情境。
- 限制：這些測試不會打開瀏覽器，**畫面顯示還是要自己在頁面上點過一次**。

### 用 Git 保存進度（只存在自己電腦，不上線）

```
git status                      # 看有哪些檔案被修改
git add .                       # 把修改加入這次要保存的內容
git commit -m "V01.02.00 本機測試版"   # 存成一個版本
```

- **先不要執行 `git push`**。push 之後，GitHub 上才會有這些修改。
- 就算 push 的是 `dev` 分支，也不會更新正式網站，因為正式網站只看 main 分支。但在準備上線之前，建議連 push 都先不要做。
- `dev/data/`（假試算表資料）已經寫在 `.gitignore` 裡，不會被 commit。

---

## 8.（可選）雲端測試環境：用真的 Google 試算表，但不是正式那份

本機的假試算表只模擬了常用的功能，**格式、顏色、公式計算結果要在真的 Google 試算表上才看得到**。想確認這些時，可以建立一份「試算表副本」來測試：

1. 打開正式的 Google 試算表 →「檔案 → 建立副本」，命名為「龍安棒球隊（測試）」。
   - 副本會連同 Apps Script 一起複製，而且是一個**獨立的**新專案，改它不會影響正式的那份。
2. 在副本裡：
   - 刪掉從正式版複製過來的比賽分頁（留下「球員名單」即可）。
   - 打開「擴充功能 → Apps Script」，把本機的 `gas/Code.gs` 貼上覆蓋，再用「＋ → 指令碼」新增 `Auth`、`Pitchers` 兩個檔案並貼上內容。
3. 在副本的 Apps Script 左側點「專案設定 ⚙️」，在「指令碼屬性」新增一筆：`TEAM_PASSCODE` = 你自訂的測試密碼。
4. 按「部署 → 新增部署作業 → 類型選『網頁應用程式』」：
   - 執行身分：**我**
   - 誰可以存取：**所有人**
   - 按部署，第一次會要求授權，照畫面同意即可。
   - 複製產生的網址（結尾是 `/exec`）。
5. 在電腦上開啟下面這個網址（`api=` 後面換成你剛剛複製的網址）：
   ```
   http://localhost:8080/lineup.html?api=https://script.google.com/macros/s/XXXXXXXX/exec
   ```
   頁首應該會顯示紫色「🧪 測試 GAS」。
6. 之後在副本的 Apps Script 修改程式時，每次都要「部署 → 管理部署作業 → 編輯 → 版本選『建立新版本』」，修改才會生效。

> 為了安全，`?api=` 只接受 `script.google.com` 開頭的網址。

---

## 9. 常見問題

| 狀況 | 解法 |
|---|---|
| 終端機顯示「'node' 不是內部或外部命令」 | Node.js 沒裝好，或裝完沒重開 VS Code／電腦 |
| 顯示「連接埠 8080 已被占用」 | 伺服器可能已經開著（找找看有沒有另一個黑色視窗）。或把 `dev/config.json` 的 `port` 改成 `8081`，網址也要跟著改成 `:8081` |
| 頁面一片空白 | Vue／Tailwind 是從網路載入的，**本機開發還是需要連上網路**。按 F12 看 Console 有沒有錯誤 |
| 頁首沒有紫色標籤 | 確認網址是 `http://localhost:8080/...`，不是 `file:///...` |
| 頁首是紅色「⚠️ 正式資料」 | 網址帶了 `?api=prod`，把它拿掉 |
| 「同步名單」後看不到球員 | 檢查 `dev/roster.json` 的格式（逗號、引號），伺服器視窗會顯示錯誤 |
| 上傳時跳出「伺服器回應格式錯誤」 | 看伺服器視窗的紅字，通常是 `.gs` 程式有錯 |

**⚠️ 特別注意：**

- `stats.html`（舊的成績登記頁）已於 2026-09-20 移除。它送出的 `savePlayerStats` 在 `Code.gs` 沒有對應分支，會顯示「成功」但成績一個字都不會寫入，留著比移除危險。個人成績改由文字紀錄轉換工具接手（見 `文字記錄簡碼字典.md`）。
- 在正式網站（github.io）上操作，永遠都是正式資料。

---

## 10. 之後要上線時（現在先不用做）

確認本機（以及雲端測試環境）都沒問題後，再依照 `docs/CHANGELOG.md` 裡的「部署順序」進行：

1. 更新正式 Apps Script，並建立新版本部署。
2. 合併到 main 分支並 push，GitHub Pages 會自動更新。

上線那天可以再請 Claude 帶你一步一步做。

> Claude 在雲端做好的改動，會以 PR（Pull Request）的形式交給你，上線流程見第 11 節。

---

## 11. Claude 做好 PR 之後：本機測試 → Merge 上線

Claude（雲端）改好程式後，會推到一個 `claude/...` 開頭的分支，並開一個 PR。
**在你按 Merge 之前，正式網站完全不受影響。**

### 11-1 先找到分支名稱

打開 PR 頁面（Claude 會給你連結，或到 GitHub repo →上方「Pull requests」分頁找）。
標題下方有一行「…wants to merge … into `main` from `claude/xxxxx`」，最後那個就是分支名稱。

### 11-2 把分支抓到電腦上

VS Code 打開專案資料夾 →「終端機 → 新增終端」，輸入（`<分支名稱>` 換成上一步看到的）：

```
git fetch origin
git switch <分支名稱>
```

- 出現「你有未 commit 的修改」而切不過去時，**不要強制切換**，把訊息貼給 Claude
- Claude 之後又推了修正到同一個 PR 時，在這個分支上執行 `git pull` 就會拿到最新版

### 11-3 啟動本機伺服器並測試

跟第 2 節一樣，二選一：

- 點兩下 `dev/start.bat`
- 或在終端機輸入 `node dev/server.js`，再開 http://localhost:8080/lineup.html

頁首要看到紫色「🧪 本機假資料」，才代表資料只寫進本機假試算表。
手機測試：連同一個 Wi-Fi，開伺服器視窗印出的「手機（同一個 Wi-Fi）」網址。

**要測什麼**：看 PR 說明的「上線步驟」，或 `docs/CHANGELOG.md` 最新版本段落的「部署」。
測完在伺服器視窗按 `Ctrl + C` 關掉。有問題就跟 Claude 說「哪一步、畫面長怎樣」，
Claude 會推修正到同一個 PR，回到 11-2 用 `git pull` 再測一次。

### 11-4 Merge 上線（GitHub 網頁，手機也可以）

1. **如果這次有改 `gas/*.gs`**：先把 .gs 貼進正式 Apps Script，「部署 → 管理部署作業 → 編輯 → 版本選『建立新版本』」。
   **GAS 一定要先部署，再 Merge**，否則操作者會卡住。（CHANGELOG 會寫明「GAS 不用動」或要動哪些檔）
2. 打開 PR 頁面，捲到最下面按綠色 **Merge pull request** → **Confirm merge**
3. 約一分鐘後正式網站更新。用手機開正式網址確認一次

### 11-5 電腦切回 main

```
git switch main
git pull origin main
```

之後你自己在電腦上改東西，就是從最新的 main 開始。

---

# 對外官網的維護（階段 C 起）

對外三頁（`index.html`、`news.html`、`schedule.html`）讀的是 repo 裡的兩個靜態檔，
**不打 GAS、不碰試算表**。改完推上 GitHub，約一分鐘後官網自動更新（GitHub Pages 會自動部署，
沒有另外的「發布」按鈕）。

| 檔案 | 內容 |
|---|---|
| `data/announcements.json` | 公告 |
| `data/schedule.json` | 比賽與練球時程 |

## 三種維護方式

**A. 口述給 Claude** —— 講一句「發一則公告，標題 X、內容 Y」，Claude 改檔並 commit，你 `git push`。

**B. 自己在 GitHub 網頁改（手機也可以，不用電腦）**

1. 開 https://github.com/a24627622-dev/LongAn_baseball_roster
2. 點進 `data/` → 要改的檔案
3. 右上角鉛筆圖示 → 直接編輯
4. 捲到最下面按 **Commit changes**（這一步就等於發布了，不用再 push）

**C. 兩個產生器（推薦，不用手寫 JSON）**

| 工具 | 管哪個檔案 | 能做什麼 |
|---|---|---|
| `/tools/announcement-builder.html` | `announcements.json` | 新增／刪除公告、切換置頂 |
| `/tools/schedule-editor.html` | `schedule.json` | 改地點與備註、標記取消、練球↔比賽互換、填比分、新增／刪除活動 |

兩個都是：填好 → 產生**整份檔案**的正確內容 → 複製 → 貼到 B 的編輯框 → Commit changes。
它們只做文字格式化，**不會寫入任何資料**，所以不需要登入也沒有金鑰風險。

> ### ⚠️ 貼上的正確做法：`Ctrl+A` 然後直接貼
>
> **不要用滑鼠拖曳選取舊內容再刪除。** 拖曳很容易漏掉開頭的 `[` 或結尾那一段，
> 貼上去就會變成「新內容 + 舊殘骸」。
>
> 正確步驟：**點進編輯框 → `Ctrl+A`（全選）→ 直接貼上**。貼上會取代全選的內容，
> 一個動作完成，不可能有殘留。手機是長按 → 全選 → 貼上。
>
> 2026-09-21 就因為拖曳選取漏刪，連續弄壞兩次：
>
> - 第一次開頭多一個 `[` → JSON 解析失敗 → 首頁和時程頁顯示「資料暫時讀不到」。
>   **這種壞法會大聲報錯，反而好發現。**
> - 第二次補上逗號讓它變成合法 JSON，但整份舊陣列還巢狀在裡面 →
>   前端把那個陣列當成一筆活動，因為它沒有 `date` 欄位而默默丟掉 →
>   **最近的那一筆整個消失，頁面完全不報錯。** 這種壞法危險得多。
>
> 改完之後**一定要自己開官網看一眼**：首頁大版位的日期對不對、時程頁的筆數對不對。

時程編輯器的兩個細節：

- 勾著「集合時間 = 開賽前一小時」時，改開賽時間集合會自動跟著動；要設例外就把勾取消
- 練球改成比賽時，開賽時間會重設成 10:00（不沿用練球的 08:00，否則集合會被算成 07:00）

## 公告格式

```json
[
  {
    "date": "2026-09-20",
    "title": "秋季聯賽開打",
    "body": "第一場 10/25 對 ZERO。\n集合時間 9:00。",
    "pinned": false
  }
]
```

- 新的放前面；`pinned: true` 會排到最前
- `body` 用 `\n` 換行，網站上會自動分成段落
- **少一個逗號整個公告區就讀不出來**。前端有防護，只會在公告區顯示「暫時讀不到」，
  首頁其他部分照常運作 —— 但還是建議用產生器避免手寫

## 時程格式

球隊慣例：**活動固定在週日，有比賽就不練球，沒比賽就練球 08:00~12:00。**

`data/schedule.json` **只預先產生約八週**（目前到 2026-11-15）。快用完時用時程編輯器往後補，
比賽賽程公布後把該週日改成 `game`。

> 為什麼不一次產到年底：產得太遠等於對隊員保證一堆你其實還不確定的活動
> （年底、跨年到底練不練球？）。八週夠用，又不會宣稱自己不知道的事。

```json
{
  "type": "game",
  "date": "2026-10-25",
  "gatherTime": "09:00",
  "startTime": "10:00",
  "venue": "大漢B球場",
  "opponent": "ZERO",
  "score": null,
  "resultUrl": null,
  "cancelled": false,
  "note": ""
}
```

```json
{
  "type": "practice",
  "date": "2026-11-08",
  "startTime": "08:00",
  "endTime": "12:00",
  "venue": "",
  "cancelled": false,
  "note": ""
}
```

規則：

- **星期和「已結束／未開始」都不存**，前端從 `date` 算，避免資料自相矛盾
- `venue` 留空白 → 網站顯示「地點未定，請留意公告」
- `gatherTime` 慣例是開賽前一小時；存下來是為了保留例外的彈性
- 比賽結束後手動填 `"score": { "longan": 7, "opponent": 3 }`，時程頁會自動判斷勝敗和
- **`resultUrl` 一律 `null`**，前端不會渲染成連結。這是預留給階段 E 賽事成績頁的 hook
- **取消**：把該筆改成 `"cancelled": true`，在 `note` 寫原因。時程頁會顯示灰底刪除線和紅色
  「已取消」標籤，首頁大版位會自動跳過它
- **改期**：原本那筆標 `cancelled`，另外新增一筆新日期（兩筆都會出現在時程頁，
  搭配一則公告說明）

## 首頁大版位的行為

- 顯示第一個「日期 >= 今天且未取消」的活動，比賽和練球都可能佔據
- 標題自動切換：落在本週（到本週日為止）寫「本週活動」，已跨到下週寫「下次活動」
- 比賽是深黑＋紅邊，練球是白底＋金邊
- 旁邊另外釘一張「下一場比賽」小卡；如果大版位已經是那場比賽就不重複顯示
- 活動當天一直顯示到午夜，隔天自動換下一個

## 本機預覽

```
node dev/server.js
```

→ http://localhost:8080/（會印出區網網址，手機可以連）
